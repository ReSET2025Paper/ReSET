grid_size: 20
visualize: true
task: pick_berry
# data_path: ./human_video/${task}
data_path: ./robot_video/${task}/correction
video_specs:
  resize: [224,224]

device: cuda:0
tracker_checkpoint: ./checkpoints/scaled_offline.pth

hydra:
  output_subdir: null
  run:
    dir: .
